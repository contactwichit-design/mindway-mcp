# PRE-DEPLOYMENT REPORT — Mindway MCP v1.0.0

Status: `READY_FOR_OWNER_DEPLOYMENT`

## Independent runtime evidence

Main QC promoted this package only after GitHub-hosted runtime execution on the repository source itself.

Validated baseline commit: `ede7f4c78f4913186af1c6b26905a73d21897242`
Permanent Runtime QC run: `31225215723`
Result: `PASS`

Environment used by GitHub Actions:

- Ubuntu 24.04 hosted runner
- Node.js 20.20.2
- npm 10.8.2
- Docker 28.0.4

Mandatory gates verified:

- Authentic committed `package-lock.json` with `lockfileVersion: 3`: PASS
- `npm ci`: PASS
- TypeScript build (`npm run build`): PASS
- Unit + integration suite (`npm test`): PASS — 17/17 tests
- Dedicated MCP integration suite (`npm run test:integration`): PASS — 5/5 tests
- Offline source/secret audit: PASS — 0 defects, 0 secret leaks
- Production dependency audit: PASS — 0 vulnerabilities
- Full dependency critical audit after Vitest toolchain upgrade: PASS
- Docker image build: PASS
- Container health check: PASS
- Official MCP SDK client smoke test against the running container: PASS

## Architecture verified

- Remote MCP endpoint uses the official `@modelcontextprotocol/sdk` production API.
- HTTP transport uses `StreamableHTTPServerTransport`.
- Integration and container smoke tests use the official MCP `Client` and `StreamableHTTPClientTransport`.
- Six Mindway tools are registered through the MCP SDK.
- Resources and the `mindway_start` prompt are registered through the MCP SDK.
- Runtime source uses Node.js >=20.
- Production container installs production dependencies only in the runtime stage.
- Default deployment documentation keeps Cloud Run private (`--no-allow-unauthenticated`) unless an explicit public-staging decision is made.

## Security and dependency correction

Earlier mock/manual lockfiles were rejected and removed. The committed lockfile was generated by npm on GitHub-hosted infrastructure and subsequently validated with `npm ci`.

The permanent CI also exposed vulnerabilities in the older Vitest/Vite/esbuild development toolchain. Vitest was upgraded to `^4.1.10`, a new authentic lockfile was generated, and the entire gate chain was rerun successfully. Production dependencies remained at zero reported vulnerabilities throughout the final validation.

## Permanent evidence gate

`.github/workflows/ci.yml` is the standing Runtime QC gate for pushes to `main`, pull requests, and manual workflow dispatch. It checks:

1. committed authentic lockfile
2. reproducible install
3. build
4. unit tests
5. MCP integration tests
6. source/secret audit
7. production dependency audit
8. full critical dependency audit
9. Docker build
10. container + official MCP client smoke test

Any future source change must pass this gate before deployment.

## Remaining owner-gated production actions

No known source/runtime blocker remains in the pre-deployment package.

Actual Cloud Run deployment, GCP billing/project selection, IAM/authentication choice, and connecting the resulting production MCP URL to Gemini Spark remain owner-gated production actions under the Mindway/B2 approval rules.

Promotion status: `READY_FOR_OWNER_DEPLOYMENT`
